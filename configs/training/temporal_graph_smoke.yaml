training:
  type: temporal_graph_encoder
  hidden_dim: 16
  batch_size: 1
  learning_rate: 0.005
  weight_decay: 0.0
  epochs: 1
  num_negatives: 1
  device: cpu
  seed: 2026
  checkpoint_dir: outputs/runs/phase6_temporal_graph_smoke/checkpoints
  reportable: false

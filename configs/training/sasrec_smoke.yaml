training:
  type: sasrec
  hidden_dim: 16
  num_layers: 1
  num_heads: 1
  dropout: 0.1
  max_seq_len: 5
  batch_size: 4
  learning_rate: 0.005
  weight_decay: 0.0
  epochs: 1
  num_negatives: 1
  device: cpu
  seed: 2026
  checkpoint_dir: outputs/runs/phase6_sasrec_smoke/checkpoints
  reportable: false
